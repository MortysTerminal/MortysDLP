# Tests

    dotnet test                                         # alle
    dotnet test --filter FullyQualifiedName~AppPaths     # einzelne Klasse

Testnamen: `Methode_Situation_ErwartetesVerhalten` (deutsch).
Keine Zufallswerte, keine Uhrzeit, keine Netzverbindung.
Dateisystemtests nur in einem temporären Ordner, der danach gelöscht wird.
